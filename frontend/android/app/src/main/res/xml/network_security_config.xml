<?xml version="1.0" encoding="utf-8"?>
<network-security-config>
    <!--
        Production backend uses HTTPS on Render — no cleartext needed.
        HTTPS traffic is allowed by default on Android; this config is a
        safety-net for any remaining local-dev / emulator usage only.
    -->
    <base-config cleartextTrafficPermitted="false" />

    <!-- Allow HTTP only for localhost / emulator during local development -->
    <domain-config cleartextTrafficPermitted="true">
        <domain includeSubdomains="false">localhost</domain>
        <domain includeSubdomains="false">127.0.0.1</domain>
        <domain includeSubdomains="false">10.0.2.2</domain>
    </domain-config>
</network-security-config>
